package com.alexgo.week_4.book;

/**
 * 1.5.30 Histogram.
 * Suppose that the standard input stream is a sequence of double values.
 * Write a program that takes an integer n and two real numbers lo and hi as
 * command-line arguments and uses StdDraw to plot a histogram of the count of the
 * numbers in the standard input stream that fall in each of the n intervals defined by
 * dividing (lo, hi) into n equal-sized intervals.
 *
 * Created by ag on 02-Aug-20 3:36 PM
 */
class Ex30_Histogram {
  public static void main(String[] args) {
    int n = Integer.parseInt(args[0]);
    double lo = Double.parseDouble(args[1]);
    double hi = Double.parseDouble(args[2]);

  }
}
